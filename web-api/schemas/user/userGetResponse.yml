---
userGetResponse:
  allOf:
    - $ref: "../../schemas.yml#/schemas/result"
    - type: object
      properties:
        data:
          allOf:
            - "$ref": "../../schemas.yml#/schemas/user"
            - type: object
              properties:
                orders:
                  type: array
                  items:
                    type: object
                    properties:
                      id:
                        type: integer
                        format: int64
                      totalCost:
                        type: number
                createdAt:
                  type: Filtered
                  description: data filtered
                updatedAt:
                  type: Filtered
                  description: data filtered
    
    